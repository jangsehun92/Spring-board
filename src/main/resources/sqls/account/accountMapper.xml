<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="accountMapper">	
	<resultMap type="Account" id="AccountResultMap">
		<result property="email" column="email"/>
		<result property="password" column="password"/>
		<result property="name" column="name"/>
		<result property="birth" column="birth"/>
		<result property="nickname" column="nickname"/>
		<result property="locked" column="locked"/>
		<result property="enabled" column="enabled"/>
		<result property="role" column="user_role"/>
		<result property="failureCount" column="failure_count"/>
		<result property="regdate" column="regdate"/>
		<result property="lastLoginDate" column="last_login_date"/>
	</resultMap>

	<insert id="save" parameterType="AccountCreateDto">
		INSERT INTO 
			account_table values(#{email},#{password},#{name},#{birth},#{nickname},0,0,#{role},0,SYSDATE, NULL)
	</insert>

	<select id="findByEmail" parameterType="java.lang.String" resultMap="AccountResultMap">
		SELECT 
			email,
			password,
			name,
			birth,
			nickname,
			locked,
			enabled,
			user_role,
			failure_count,
			regdate,
			last_login_date
		FROM 
			account_table
		WHERE 
			email = #{email}
	</select>
	
	<update id="updatePassword" parameterType="Account">
		UPDATE 
			account_table
		SET 
			locked = 0,
			password = #{password}
		WHERE
			email = #{email}
	</update>
	
	<select id="findAccount" parameterType="AccountFindRequestDto" resultType="AccountFindResponseDto">
		SELECT
			email,
			regdate
		FROM 
			account_table
		WHERE
			name = #{name} AND
			birth = #{birth}
	</select>
	<!-- 로그인 성공 시 마지막로그인날짜 저장 -->
	<update id="updateLoginDate" parameterType="java.lang.String">
		UPDATE
			account_table
		SET	
			last_login_date = SYSDATE
		WHERE
			email = #{email}
	</update>
	
	<select id="failureCount" parameterType="java.lang.String" resultType="int">
		SELECT
			failure_count
		FROM
			account_table
		WHERE
			email = #{email}
	</select>
	
	<!-- 로그인 실패&성공 시 실패카운트 증가 및 초기화 -->
	<update id="updateFailureCount" parameterType="map">
		UPDATE 
			account_table
		SET 
			failure_count = #{failureCount}
		WHERE
			email = #{email}
	</update>
	
	<!-- 계정 잠금(locekd = 1) -->
	<update id="updateLocked" parameterType="map">
		UPDATE 
			account_table
		SET 
			locked = #{locked},
			failure_count = 0
		WHERE
			email = #{email}
	</update>
	
	<select id="emailCheck" parameterType="java.lang.String" resultType="int">
		SELECT 
			count(email)
		FROM 
			account_table
		WHERE 
			email = #{email}
	</select>
	
	<update id="activetion" parameterType="java.lang.String">
		UPDATE 
			account_table
		SET
			enabled = 1
		WHERE
			email = #{email}
	</update>
	
</mapper>